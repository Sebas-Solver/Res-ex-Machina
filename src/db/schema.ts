/**
 * Schema de la base de datos — tabla records.
 *
 * La implementación completa se hará en el Issue #2.
 * Este archivo es un placeholder que permite que el proyecto compile.
 *
 * Referencia: PRD v1.1 sección G (Modelo de datos)
 */

// TODO: Issue #2 — Implementar tabla records con todos los constraints
// - content_hash CHECK
// - state CHECK
// - visibility CHECK
// - UNIQUE (agent_wallet, nonce)
// - UNIQUE fee_tx_hash
// - UNIQUE content_hash
// - Índices
